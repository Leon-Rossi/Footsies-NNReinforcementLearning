using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ToDoList : MonoBehaviour
{
    //AIControl script contains 2 NNs and cycles through them using bubble sort (or similar)
    //Battle core line 298, GetInputData gets inputs from aiontrol script if public bool is enabled
}
